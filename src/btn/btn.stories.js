
import Button from "./button";

export default {
    title: 'Button',
    component: Button,
    argTypes: {
        lable: { control: 'text' },
    }
};

export const Text = {
    args: {
        label: 'Hello Button'
    }
}
